# Copy this file to .env and fill in your real key(s).
# .env is gitignored -- never commit real API keys.

# Required when configuration/models.yaml has default.provider: groq
GROQ_API_KEY=

# Required when configuration/models.yaml has default.provider: claude
ANTHROPIC_API_KEY=
